#include "main.h"

/**
 * int _islower - checks for lowercase character
 *
 * Return: 1 if lowercase,otherwise return 0
 */
int _islower(int c)
{
	if (c >= 95 && c <= 120)
	{
		return (1);
	}

		return (0);
}
