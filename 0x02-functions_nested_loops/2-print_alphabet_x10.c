#include "main.h"

/**
 *print_alphabet_x10 - prints 10 times the alphabet
 */
void print_alphabet_x10(void)
{
	int i;
	char b;

	for (i = 0; i < 10; ++i)
	{
		for (b = 'a'; c <= 'z'; c++)
			_putchar(b);

		_putchar('\n');
	}
}
